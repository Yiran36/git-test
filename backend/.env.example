# MySQL（Docker Compose 默认配置）
DB_HOST=localhost
DB_PORT=3306
DB_USER=subject_user
DB_PASSWORD=subject_pass
DB_NAME=subject_ai

# 后端服务端口
PORT=3000

# 第 9 节使用 mock；第 10 节可改为 deepseek
AI_PROVIDER=mock

# 第 10 节 DeepSeek API Key（第 9 节留空，切勿提交真实 Key）
# DEEPSEEK_API_KEY=
